# Keep the build context small and never ship local state into the image.
node_modules
.next
out
build
.git
.gitignore

# local database and secrets
prisma/*.db
prisma/*.db-journal
.env
.env.*
!.env.example

server.log
npm-debug.log*
*.tsbuildinfo
.DS_Store
README.md
